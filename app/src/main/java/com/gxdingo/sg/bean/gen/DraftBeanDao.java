package com.gxdingo.sg.bean.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.gxdingo.sg.db.bean.DraftBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DRAFT_BEAN".
*/
public class DraftBeanDao extends AbstractDao<DraftBean, Long> {

    public static final String TABLENAME = "DRAFT_BEAN";

    /**
     * Properties of entity DraftBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Uuid = new Property(1, String.class, "uuid", false, "UUID");
        public final static Property Draft = new Property(2, String.class, "draft", false, "DRAFT");
        public final static Property SendIdentifier = new Property(3, String.class, "sendIdentifier", false, "SEND_IDENTIFIER");
    }


    public DraftBeanDao(DaoConfig config) {
        super(config);
    }
    
    public DraftBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DRAFT_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"UUID\" TEXT UNIQUE ," + // 1: uuid
                "\"DRAFT\" TEXT," + // 2: draft
                "\"SEND_IDENTIFIER\" TEXT);"); // 3: sendIdentifier
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DRAFT_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DraftBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindString(2, uuid);
        }
 
        String draft = entity.getDraft();
        if (draft != null) {
            stmt.bindString(3, draft);
        }
 
        String sendIdentifier = entity.getSendIdentifier();
        if (sendIdentifier != null) {
            stmt.bindString(4, sendIdentifier);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DraftBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindString(2, uuid);
        }
 
        String draft = entity.getDraft();
        if (draft != null) {
            stmt.bindString(3, draft);
        }
 
        String sendIdentifier = entity.getSendIdentifier();
        if (sendIdentifier != null) {
            stmt.bindString(4, sendIdentifier);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public DraftBean readEntity(Cursor cursor, int offset) {
        DraftBean entity = new DraftBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // uuid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // draft
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // sendIdentifier
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DraftBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUuid(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDraft(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSendIdentifier(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DraftBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DraftBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DraftBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
